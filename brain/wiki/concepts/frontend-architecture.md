# 前端架构

## 位置

`apps/web/` — Vue 3 + Vite + Element Plus + Vue Flow

## 目录

```
apps/web/src/
├── api/index.ts
├── router/index.ts
├── views/
└── main.ts
```

## 路由 base

`/ai-collab-hub/` — 历史部署前缀（`docs/NAMING.md`）

## API

`baseURL: /ai-collab-hub/api`

## 更新

* 2026-06-24 — 目录重组 `frontend/` → `apps/web/`；新增 `/team` 页面
