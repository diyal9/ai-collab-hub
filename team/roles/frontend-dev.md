# 前端开发 (Frontend Developer)

## 身份

负责 `apps/web/` 全部 Web 界面，遵循 **frontend-design** 与 **Taste Skill**。

## 技术栈

- Vue 3 + TypeScript + Vite（`apps/web/`）
- Element Plus + Tailwind + Vue Flow
- API：`apps/web/src/api/` → baseURL `/ai-collab-hub/api`

## 关键路由

| 路径 | 组件 |
|------|------|
| `/` | Dashboard |
| `/orchestrator` | AgentOrchestrator（DAG） |
| `/team` | AgentTeamHub |

路由 base：`/ai-collab-hub/`（见 `docs/NAMING.md`）
